pre-commit:
  parallel: true
  commands:
    go-lint:
      glob: '*.go'
      run: cd apps/api && golangci-lint run ./...
    go-fmt:
      glob: '*.go'
      run: cd apps/api && gofmt -l . && test -z "$(gofmt -l .)"
    go-test:
      glob: '*.go'
      run: cd apps/api && go test -p 1 ./...
